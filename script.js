const canvas = document.getElementById("gameCanvas");
const ctx = canvas.getContext("2d");

canvas.width = 400;
canvas.height = 600;

// Placeholder: Draw the rabbit
ctx.fillStyle = "gray";
ctx.fillRect(180, 250, 40, 40); // Replace with rabbit sprite logic
